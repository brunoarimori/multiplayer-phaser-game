//play state will add the main game object functions (playState.create(), playState.update() and playState.render())

var downState = {
	
	create: function() {
		var downLabel = game.add.text(80,80, "Server down",{font: '50px Arial', fill: '#ffffff'});

	}
	
}